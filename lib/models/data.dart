class Data {
  int ? id;
  String ? nama;
  String ? gambar;
  String ? deskripsi;
  int ? jumlah;
  int ? harga;

  Data({
    this.id,
    this.nama,
    this.gambar,
    this.deskripsi,
    this.jumlah,
    this.harga,
  });
}
